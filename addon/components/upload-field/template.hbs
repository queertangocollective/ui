{{#if isLoading}}
  {{icon 'spinner'}}
{{else if file}}
  {{yield file (action 'remove')}}
{{else}}
  {{#file-upload name=name accept="image/*" multiple=multiple onfileadd=(action onchange)}}
    {{yield to='inverse'}}
  {{/file-upload}}
{{/if}}

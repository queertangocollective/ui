{{#animated-container}}
  {{#form-for this.model onsubmit=(action 'submit') as |f|}}
    {{#animated-if this.isCreating use=fade duration=200}}
      {{f.text this.field hasLabel=false autofocus=true type=type placeholder=placeholder oncancel=(action clear)}}
    {{/animated-if}}

    {{#if this.isCreating}}
      {{#f.submit class='icon'}}
        {{yield to='inverse'}}
      {{/f.submit}}
    {{else}}
      <button type='submit' onclick={{action 'create'}}>
        {{yield}}
      </button>
    {{/if}}
  {{/form-for}}
{{/animated-container}}
